# agent-brain MCP Backend Implementation Plan (Plan A)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build `@agent-brain/mcp` + `@agent-brain/shared` — a working MCP server (stdio + HTTP) with SQLite + Drizzle + FTS5, E5-base embeddings, AST chunker, 12 tools, and a full contract test suite. The UI (`@agent-brain/ui`) is Plan B and consumes this backend unchanged.

**Architecture:** Monorepo pnpm. Single Node process that is either (a) an MCP stdio server for Claude Code / Cursor, or (b) an Express 5 JSON-RPC server (`--ui` flag) bound to `127.0.0.1`. One SQLite file at `~/.agent-brain/brain.db`. Embeddings local via `@huggingface/transformers` (Xenova/multilingual-e5-base). Chunking via `unified` + `remark-parse` AST walk.

**Tech Stack:** TypeScript strict, pnpm workspaces, better-sqlite3, drizzle-orm, drizzle-zod, @modelcontextprotocol/sdk, zod, express 5, @huggingface/transformers, unified + remark-parse + remark-gfm, vitest, tsup, tsx.

**Spec reference:** `docs/superpowers/specs/2026-04-08-agent-brain-mvp-design.md`. Tasks refer to spec sections (e.g. "§ 2.1") when detailed specifications are already captured there.

**Out of scope for Plan A:** everything in `packages/ui/`. The `http/` module is implemented and serves `/mcp` JSON-RPC, but there are no React assets yet and Express serves an HTML stub on `GET /` with a "UI not built — run Plan B".

---

## File structure (Plan A scope)

```
agent-brain/
├── package.json                                     (root, name: agent-brain-workspace)
├── pnpm-workspace.yaml
├── tsconfig.base.json
├── .gitignore                                       (+ ~/.agent-brain/ stays out of repo anyway)
├── CLAUDE.md                                        (references gotchas table)
├── packages/
│   ├── shared/
│   │   ├── package.json                             @agent-brain/shared, no build
│   │   ├── tsconfig.json
│   │   └── src/
│   │       ├── index.ts                             re-exports
│   │       ├── constants.ts                         GLOBAL_PROJECT_ID, limits
│   │       ├── db/
│   │       │   └── schema.ts                        Drizzle tables (lives here so drizzle-zod can consume them without circular dep)
│   │       ├── schemas.ts                           Zod schemas via createSelectSchema + response envelopes
│   │       └── types.ts                             z.infer exports
│   └── mcp/
│       ├── package.json                             @agent-brain/mcp, bin, tsup build
│       ├── tsconfig.json
│       ├── tsup.config.ts
│       ├── vitest.config.ts
│       └── src/
│           ├── index.ts                             CLI entry: stdio | --ui
│           ├── config.ts                            CHUNK_CONFIG, paths
│           ├── db/
│           │   ├── index.ts                         openDatabase() with all pragmas
│           │   ├── schema.ts                        Drizzle schema (5 tables)
│           │   ├── migrations/                      auto-generated by drizzle-kit
│           │   │   ├── 0000_init.sql
│           │   │   └── meta/
│           │   ├── migrate.ts                       apply migrations + seed + FTS5 DDL + view
│           │   ├── ddl.ts                           hand-written SQL for FTS5, triggers, view
│           │   ├── seed.ts                          global project seed
│           │   ├── backup.ts                        checkpointAndCopy() helper
│           │   └── hash.ts                          sha256 helper
│           ├── embeddings/
│           │   ├── pipeline.ts                      getEmbeddingPipeline() memoized
│           │   ├── embed.ts                         embedPassage / embedQuery wrappers
│           │   ├── tokenizer.ts                     AutoTokenizer.from_pretrained E5
│           │   └── chunking.ts                      AST-based C light chunker
│           ├── tools/
│           │   ├── index.ts                         registerAllTools(server)
│           │   ├── contracts.ts                     Zod response shapes (import from shared)
│           │   ├── projects.ts                      4 project tools
│           │   ├── notes.ts                         5 note tools
│           │   ├── search.ts                        unified search tool
│           │   └── maintenance.ts                   backup_brain, get_stats
│           ├── notes/
│           │   └── sync-chunks.ts                   diff-based chunk sync
│           ├── search/
│           │   ├── fulltext.ts                      FTS5 MATCH + BM25
│           │   ├── semantic.ts                      view query + cosine
│           │   ├── hybrid.ts                        RRF combiner
│           │   └── neighbors.ts                     neighbor expansion loader
│           ├── http/
│           │   ├── server.ts                        Express 5 + JSON-RPC handler
│           │   ├── health.ts                        /health endpoint
│           │   └── jsonrpc.ts                       minimal JSON-RPC dispatcher to tools
│           ├── scripts/
│           │   ├── restore.ts                       pnpm mcp:restore CLI
│           │   └── reindex.ts                       pnpm mcp:reindex CLI
│           └── tests/
│               ├── setup.ts                         vi.mock @huggingface/transformers
│               ├── _helpers/
│               │   ├── mock-embeddings.ts           deterministic fake 768d
│               │   ├── fresh-db.ts                  :memory: DB per test
│               │   └── factories.ts                 makeProject, makeNote, ...
│               ├── db/
│               │   ├── schema.test.ts
│               │   ├── cascade.test.ts
│               │   ├── backup.test.ts
│               │   └── serialization.test.ts
│               ├── embeddings/
│               │   └── chunking.test.ts
│               ├── notes/
│               │   └── sync-chunks.test.ts
│               ├── search/
│               │   └── hybrid.test.ts
│               └── tools/
│                   ├── contracts.test.ts
│                   ├── projects.test.ts
│                   ├── notes.test.ts
│                   └── search.test.ts
└── docs/superpowers/
    ├── specs/2026-04-08-agent-brain-mvp-design.md   (exists)
    └── plans/2026-04-08-agent-brain-mcp-backend.md  (this file)
```

---

## Conventions used throughout

- **TDD**: every task with runtime logic follows Red → Green → Commit. Test first.
- **Commits**: one commit per task (end of task). Message prefixes: `feat:`, `test:`, `chore:`, `refactor:`, `docs:`.
- **Type aliasing**: tests and internal helpers import types from `@agent-brain/shared`, never redefine.
- **No Date.now() in tests**: use `vi.useFakeTimers()` + fixed epoch when deterministic timestamps matter.
- **SQL in `ddl.ts`**: all hand-written SQL (FTS5 virtual tables, triggers, views) lives in one file, applied after drizzle migrations.

Task index:

| # | Task | Area |
|---|---|---|
| 0  | Worktree + repo bootstrap                     | infra |
| 1  | pnpm monorepo skeleton + tsconfig              | infra |
| 2  | `@agent-brain/shared` constants & limits       | shared |
| 3  | `@agent-brain/shared` Drizzle schema (5 tables) | shared |
| 4  | `@agent-brain/shared` Zod schemas via createSelectSchema + response envelopes | shared |
| 5  | `@agent-brain/mcp` package skeleton            | mcp |
| 6  | `openDatabase()` + pragmas                     | db |
| 7  | Migrations runner + DDL file (FTS5, view)      | db |
| 8  | Global project seed                            | db |
| 9  | Triggers (FTS5 + embeddings orphan cleanup)    | db |
| 10 | Cascade invariant tests                        | db |
| 11 | Backup helper + test                           | db |
| 12 | Test setup: mock-embeddings helper             | testing |
| 13 | Test setup: fresh-db helper + factories        | testing |
| 14 | Embeddings pipeline wrapper                    | embeddings |
| 15 | E5 tokenizer wrapper                           | embeddings |
| 16 | AST chunker (C light) — happy path            | chunking |
| 17 | AST chunker — fenced code + round-trip equality invariant | chunking |
| 18 | AST chunker — paragraph + sentence fallback   | chunking |
| 18b| AST chunker — real-E5 smoke test (opt-in, gotcha 12) | chunking |
| 19 | `project_*` tools (4) + tests                  | tools |
| 20 | Contract test framework                        | tools |
| 21 | `note_create` (atom path) + test               | tools |
| 22 | `note_create` (document path) + test           | tools |
| 23 | Diff-based `sync-chunks` + unit tests          | notes |
| 24 | `note_get` / `note_delete` / `note_list`       | tools |
| 25 | `note_update` wired to sync-chunks             | tools |
| 26 | Serialization round-trip test (tags JSON)      | db |
| 27 | Fulltext search (FTS5 + filters)               | search |
| 28 | Semantic search (view + cosine + filters)      | search |
| 29 | Hybrid RRF + neighbor expansion                | search |
| 30 | `search` tool + tests                          | tools |
| 31 | `backup_brain` + `get_stats` tools             | tools |
| 32 | MCP stdio server wiring                        | server |
| 33 | Express HTTP + JSON-RPC bridge                 | server |
| 34 | `--ui` flag, `/health`, embedder preload       | server |
| 35 | `app_meta` + CHUNK_CONFIG version guard        | db |
| 36 | CLI scripts: `mcp:restore`, `mcp:reindex`      | scripts |
| 37 | tsup build + bin + smoke test                  | build |
| 38 | README + Claude Code config + CLAUDE.md        | docs |

---

## Task 0: Worktree bootstrap

**Files:**
- Modify: none yet (verify working directory)

- [ ] **Step 1: Verify working directory is `/Users/recarnot/dev/agent-brain` and branch is clean**

Run:
```bash
cd /Users/recarnot/dev/agent-brain && git status --short && git branch --show-current
```
Expected: empty output after `git status`, branch `main` (or a feature branch if a worktree was created).

- [ ] **Step 2: Verify Node and pnpm versions**

Run:
```bash
node -v && pnpm -v
```
Expected: Node `>= v20`, pnpm `>= 9`.

- [ ] **Step 3: If anything unexpected, stop and escalate**

No commit.

---

## Task 1: pnpm monorepo skeleton + tsconfig

**Files:**
- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `tsconfig.base.json`
- Modify: `.gitignore`

- [ ] **Step 1: Create root `package.json`**

```json
{
  "name": "agent-brain-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev":        "concurrently -n mcp,ui -c blue,magenta \"pnpm --filter @agent-brain/mcp dev\" \"pnpm --filter @agent-brain/ui dev\"",
    "build":      "pnpm --filter @agent-brain/mcp build",
    "test":       "pnpm -r test",
    "typecheck":  "pnpm -r typecheck",
    "mcp:restore": "node packages/mcp/dist/scripts/restore.js",
    "mcp:reindex": "node packages/mcp/dist/scripts/reindex.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.12.0"
}
```

- [ ] **Step 2: Create `pnpm-workspace.yaml`**

```yaml
packages:
  - "packages/*"
```

- [ ] **Step 3: Create `tsconfig.base.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noImplicitAny": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "lib": ["ES2022"]
  }
}
```

- [ ] **Step 4: Update `.gitignore`**

Append to existing `.gitignore`:

```
node_modules
dist
*.tsbuildinfo
.DS_Store
.turbo
coverage
*.db
*.db-wal
*.db-shm
```

- [ ] **Step 5: `pnpm install` to populate lockfile**

Run:
```bash
pnpm install
```
Expected: lockfile `pnpm-lock.yaml` created, no package errors.

- [ ] **Step 6: Commit**

```bash
git add package.json pnpm-workspace.yaml tsconfig.base.json .gitignore pnpm-lock.yaml
git commit -m "chore: bootstrap pnpm monorepo skeleton"
```

---

## Task 2: `@agent-brain/shared` constants & limits

**Files:**
- Create: `packages/shared/package.json`
- Create: `packages/shared/tsconfig.json`
- Create: `packages/shared/src/constants.ts`
- Create: `packages/shared/src/index.ts`

- [ ] **Step 1: Create `packages/shared/package.json`**

```json
{
  "name": "@agent-brain/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./db/schema": "./src/db/schema.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"(no tests in shared)\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "drizzle-orm": "^0.36.0",
    "drizzle-zod": "^0.5.1"
  }
}
```

Note: `drizzle-orm/sqlite-core` does **not** pull `better-sqlite3`; it is a pure schema DSL. This is why we can declare Drizzle tables in `@agent-brain/shared` without polluting it with native deps. `drizzle-zod` is used in Task 4 via `createSelectSchema(notes)` — not dead weight.

- [ ] **Step 2: Create `packages/shared/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "noEmit": true
  },
  "include": ["src"]
}
```

- [ ] **Step 3: Create `packages/shared/src/constants.ts`**

```ts
export const GLOBAL_PROJECT_ID = '00000000-0000-0000-0000-000000000000' as const;

export const LIMITS = {
  MAX_TITLE: 200,
  MAX_TAG: 40,
  MAX_TAGS_PER_NOTE: 20,
  MAX_QUERY: 500,
  MAX_PROJECT_SLUG: 64,
  MAX_PROJECT_NAME: 120,
  MAX_PROJECT_DESCRIPTION: 500,
  MAX_BACKUP_LABEL: 64,
  MAX_LIST_LIMIT: 200,
  MAX_SEARCH_LIMIT: 50,
  MAX_NEIGHBORS: 3,
  MAX_CONTENT: 1_000_000,        // 1 MB markdown guard
} as const;

export const KINDS = ['atom', 'document'] as const;
export const STATUSES = ['draft', 'active', 'archived', 'deprecated'] as const;
export const SOURCES = ['ai', 'human', 'import'] as const;
export const SEARCH_MODES = ['fulltext', 'semantic', 'hybrid'] as const;
```

- [ ] **Step 4: Create `packages/shared/src/index.ts`**

```ts
export * from './constants.js';
export * from './schemas.js';
export * from './types.js';
```

Note: Zod schemas and types files come in Task 3. TypeScript will error on these re-exports until Task 3 — that's fine, we commit Task 2 stand-alone via an intermediate `index.ts`.

- [ ] **Step 5: Temporarily simplify `index.ts` so this task compiles on its own**

Replace `packages/shared/src/index.ts` with:

```ts
export * from './constants.js';
```

- [ ] **Step 6: Typecheck**

Run:
```bash
pnpm --filter @agent-brain/shared typecheck
```
Expected: no errors.

- [ ] **Step 7: Commit**

```bash
git add packages/shared/package.json packages/shared/tsconfig.json packages/shared/src/
git commit -m "feat(shared): add constants and limits"
```

---

## Task 3: `@agent-brain/shared` Drizzle schema (5 tables)

> **Rationale**: Drizzle tables live in `@agent-brain/shared` (not in `mcp`) so that `@agent-brain/shared/schemas.ts` can build Zod schemas via `createSelectSchema(notes)` — guaranteeing the Zod property names (`projectId`, `createdAt`, etc.) match what Drizzle returns at runtime. This is the **only** way to prevent the camelCase ↔ snake_case drift flagged as gotcha 3. The `drizzle-orm/sqlite-core` DSL is pure TypeScript with no native deps, so `shared` stays build-free and UI-safe.

**Files:**
- Create: `packages/shared/src/db/schema.ts`

- [ ] **Step 1:** Create `packages/shared/src/db/schema.ts` with the 5 tables from **spec § 2.1**:

```ts
import { sqliteTable, text, integer, blob, primaryKey, index, check, foreignKey } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';

export const projects = sqliteTable('projects', {
  id: text('id').primaryKey(),
  slug: text('slug').notNull().unique(),
  name: text('name').notNull(),
  description: text('description'),
  repoPath: text('repo_path'),
  color: text('color'),
  createdAt: integer('created_at').notNull(),
  updatedAt: integer('updated_at').notNull(),
});

export const notes = sqliteTable(
  'notes',
  {
    id: text('id').primaryKey(),
    projectId: text('project_id')
      .notNull()
      .default('00000000-0000-0000-0000-000000000000')
      .references(() => projects.id),
    kind: text('kind', { enum: ['atom', 'document'] }).notNull(),
    title: text('title').notNull(),
    content: text('content').notNull(),
    tags: text('tags').notNull().default('[]'),     // JSON array string
    status: text('status', { enum: ['draft', 'active', 'archived', 'deprecated'] }).notNull().default('active'),
    source: text('source', { enum: ['ai', 'human', 'import'] }).notNull(),
    contentHash: text('content_hash').notNull(),
    createdAt: integer('created_at').notNull(),
    updatedAt: integer('updated_at').notNull(),
  },
  (t) => ({
    projectIdx: index('notes_project_idx').on(t.projectId),
    kindStatusIdx: index('notes_kind_status_idx').on(t.kind, t.status),
    updatedIdx: index('notes_updated_idx').on(t.updatedAt),
  }),
);

export const chunks = sqliteTable(
  'chunks',
  {
    id: text('id').primaryKey(),
    noteId: text('note_id')
      .notNull()
      .references(() => notes.id, { onDelete: 'cascade' }),
    position: integer('position').notNull(),
    headingPath: text('heading_path'),
    content: text('content').notNull(),
    contentHash: text('content_hash').notNull(),
    createdAt: integer('created_at').notNull(),
  },
  (t) => ({
    noteIdx: index('chunks_note_idx').on(t.noteId),
    uniq: { name: 'chunks_note_position_uniq', columns: [t.noteId, t.position], unique: true } as any,
  }),
);

export const embeddings = sqliteTable(
  'embeddings',
  {
    ownerType: text('owner_type', { enum: ['note', 'chunk'] }).notNull(),
    ownerId: text('owner_id').notNull(),
    model: text('model').notNull(),
    dim: integer('dim').notNull(),
    contentHash: text('content_hash').notNull(),
    vector: blob('vector', { mode: 'buffer' }).notNull(),
    createdAt: integer('created_at').notNull(),
  },
  (t) => ({
    pk: primaryKey({ columns: [t.ownerType, t.ownerId, t.model] }),
    modelIdx: index('embeddings_model_idx').on(t.model),
  }),
);

export const appMeta = sqliteTable('app_meta', {
  key: text('key').primaryKey(),
  value: text('value').notNull(),
});
```

- [ ] **Step 2: Update `packages/shared/src/index.ts`** to re-export schema:

```ts
export * from './constants.js';
export * as schema from './db/schema.js';
```

- [ ] **Step 3:** `pnpm --filter @agent-brain/shared typecheck` → passes.
- [ ] **Step 4:** Commit: `feat(shared): Drizzle schema for 5 tables (gotcha 3 fix — schema colocated with Zod source)`.

---

## Task 4: `@agent-brain/shared` Zod schemas & types via createSelectSchema

**Files:**
- Create: `packages/shared/src/schemas.ts`
- Create: `packages/shared/src/types.ts`
- Modify: `packages/shared/src/index.ts`

- [ ] **Step 1: Create `packages/shared/src/schemas.ts`** using `drizzle-zod.createSelectSchema` so Zod property names match Drizzle's camelCase output:

```ts
import { z } from 'zod';
import { createSelectSchema } from 'drizzle-zod';
import { projects, notes, chunks } from './db/schema.js';
import { KINDS, STATUSES, SOURCES, SEARCH_MODES } from './constants.js';

// ---- Primitive aliases ----

export const UuidSchema = z.string().uuid();
export const SlugSchema = z.string().regex(/^[a-z0-9][a-z0-9-]*$/).max(64);
export const HexColorSchema = z.string().regex(/^#[0-9a-fA-F]{6}$/);
export const TimestampSchema = z.number().int().nonnegative();

// ---- Entity schemas — derived from Drizzle, camelCase everywhere ----

export const ProjectSchema = createSelectSchema(projects);

// Override `tags`: stored as JSON string in DB, exposed as string[] in the typed API.
// The MCP handlers are responsible for JSON.parse on read and JSON.stringify on write.
export const NoteSchema = createSelectSchema(notes).extend({
  tags: z.array(z.string().min(1).max(40)).max(20),
});

export const ChunkSchema = createSelectSchema(chunks);

// Note on naming: these are "wire" response shapes (not DB rows), so we pick
// camelCase everywhere to stay consistent with the Drizzle-derived entity schemas.
export const SearchHitSchema = z.object({
  ownerType: z.enum(['note', 'chunk']),
  ownerId: UuidSchema,
  score: z.number(),
  scoreFts: z.number().optional(),
  scoreSem: z.number().optional(),
  snippet: z.string(),
  parent: z.object({
    noteId: UuidSchema,
    title: z.string(),
    kind: z.enum(KINDS),
    projectId: UuidSchema,
    tags: z.array(z.string()),
    status: z.enum(STATUSES),
  }),
  chunk: z
    .object({
      headingPath: z.string(),
      position: z.number().int(),
      neighbors: z.array(
        z.object({
          position: z.number().int(),
          content: z.string(),
        }),
      ),
    })
    .optional(),
});

// ---- Response envelope factories ----

export const ListResponseSchema = <T extends z.ZodTypeAny>(item: T) =>
  z.object({
    items: z.array(item),
    total: z.number().int().nonnegative(),
    limit: z.number().int().positive(),
    offset: z.number().int().nonnegative(),
  });

export const ItemResponseSchema = <T extends z.ZodTypeAny>(item: T) =>
  z.object({ item });

export const MutationResponseSchema = <T extends z.ZodTypeAny>(item?: T) =>
  z.object({
    ok: z.literal(true),
    id: UuidSchema,
    ...(item ? { item: item.optional() } : { item: z.unknown().optional() }),
  });

export const SearchResponseSchema = z.object({
  hits: z.array(SearchHitSchema),
  total: z.number().int().nonnegative(),
  mode: z.enum(SEARCH_MODES),
});

// ---- Stats ----

export const StatsSchema = z.object({
  projects: z.number().int(),
  notes: z.object({
    total: z.number().int(),
    byKind: z.record(z.string(), z.number().int()),
    byStatus: z.record(z.string(), z.number().int()),
    bySource: z.record(z.string(), z.number().int()),
  }),
  chunks: z.object({
    total: z.number().int(),
    avgPerDoc: z.number(),
  }),
  embeddings: z.object({
    total: z.number().int(),
    byOwnerType: z.record(z.string(), z.number().int()),
    model: z.string(),
  }),
  dbSizeBytes: z.number().int(),
  topTags: z.array(z.object({ tag: z.string(), count: z.number().int() })),
});
```

- [ ] **Step 2: Create `packages/shared/src/types.ts`** — unchanged from prior revision, just `z.infer` exports.
- [ ] **Step 3: Update `packages/shared/src/index.ts`** to add `export * from './schemas.js'; export * from './types.js';`.
- [ ] **Step 4:** Typecheck passes.
- [ ] **Step 5:** Commit: `feat(shared): Zod schemas via createSelectSchema (gotcha 3 fix — camelCase throughout)`.

### types.ts content (for Step 2 above):

```ts
import { z } from 'zod';
import { ProjectSchema, NoteSchema, ChunkSchema, SearchHitSchema, StatsSchema } from './schemas.js';

export type Project = z.infer<typeof ProjectSchema>;
export type Note = z.infer<typeof NoteSchema>;
export type Chunk = z.infer<typeof ChunkSchema>;
export type SearchHit = z.infer<typeof SearchHitSchema>;
export type Stats = z.infer<typeof StatsSchema>;

export type Kind = Note['kind'];
export type Status = Note['status'];
export type Source = Note['source'];
```

---

## Tasks 5–38: Incremental build

> **Note for the implementing engineer**: tasks 4 through 38 follow the exact same TDD pattern as tasks 1–3 above. Rather than inline 1500+ lines of code here (which would duplicate the spec), each task below specifies: (a) the exact files to create/modify, (b) the spec section that contains the authoritative code or SQL, (c) the failing test to write first, (d) the command to verify it fails, (e) the command to verify it passes after implementation, and (f) the commit message. The implementing engineer MUST NOT skip the Red step — every task starts with a failing test.

### Task 5: `@agent-brain/mcp` package skeleton

**Files:**
- Create: `packages/mcp/package.json`
- Create: `packages/mcp/tsconfig.json`
- Create: `packages/mcp/tsup.config.ts`
- Create: `packages/mcp/vitest.config.ts`
- Create: `packages/mcp/src/index.ts` (stub: `console.log('agent-brain boot placeholder')`)

- [ ] **Step 1:** Create `package.json` with deps: `better-sqlite3@^11.3.0`, `drizzle-orm@^0.36.0`, `@modelcontextprotocol/sdk@^1.0.4`, `zod@^3.23.8`, `express@^5.0.0`, `@huggingface/transformers@^3.0.0`, `unified@^11.0.5`, `remark-parse@^11.0.0`, `remark-gfm@^4.0.0`, `unist-util-visit@^5.0.0`. Dev deps: `tsup`, `tsx`, `vitest@^2.1.0`, `drizzle-kit`, `@types/better-sqlite3`, `@types/express`, `@types/node`. Workspace dep: `"@agent-brain/shared": "workspace:*"`.
- [ ] **Step 2:** Create `tsconfig.json` extending `../../tsconfig.base.json`, `rootDir: "./src"`, `outDir: "./dist"`, include `["src"]`.
- [ ] **Step 3:** Create `tsup.config.ts` with `entry: ['src/index.ts', 'src/scripts/restore.ts', 'src/scripts/reindex.ts']`, `format: ['esm']`, `dts: true`, `shims: true`, `banner: { js: '#!/usr/bin/env node' }` on `src/index.ts` only.
- [ ] **Step 4:** Create `vitest.config.ts` with `test.setupFiles: ['./src/tests/setup.ts']`, `test.environment: 'node'`, `test.pool: 'threads'`.
- [ ] **Step 5:** Create `src/index.ts`: `console.log('agent-brain boot placeholder');`.
- [ ] **Step 6:** `pnpm install`, then `pnpm --filter @agent-brain/mcp typecheck` — passes.
- [ ] **Step 7:** Commit: `feat(mcp): package skeleton`.

### Task 5 (migration step): generate migrations from the shared schema

**Files:**
- Create: `packages/mcp/drizzle.config.ts`
- Create: `packages/mcp/src/db/migrations/0000_init.sql` (generated)

> The Drizzle schema already lives in `@agent-brain/shared/src/db/schema.ts` (Task 3). MCP depends on `@agent-brain/shared` via workspace protocol and re-exports it where convenient, but the migrations are generated **inside** `packages/mcp/` because they are a deployment artifact of the MCP package (served from `packages/mcp/src/db/migrations/`).

- [ ] **Step 1:** Create `packages/mcp/drizzle.config.ts`:

```ts
import type { Config } from 'drizzle-kit';
export default {
  schema: '../shared/src/db/schema.ts',
  out: './src/db/migrations',
  dialect: 'sqlite',
} satisfies Config;
```

- [ ] **Step 2:** Run `pnpm --filter @agent-brain/mcp exec drizzle-kit generate` → produces `src/db/migrations/0000_init.sql`. Verify: 5 `CREATE TABLE` statements, correct FKs, `UNIQUE(note_id, position)` on chunks, composite PK on embeddings.
- [ ] **Step 3:** Typecheck passes.
- [ ] **Step 4:** Commit: `feat(mcp/db): generate initial migrations from shared schema`.

### Task 6: `openDatabase()` helper with pragmas

**Files:**
- Create: `packages/mcp/src/db/index.ts`
- Test: `packages/mcp/src/tests/db/schema.test.ts`

- [ ] **Step 1 (Red):** Write `schema.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { openDatabase } from '../../db/index.js';

describe('openDatabase', () => {
  it('sets all required pragmas in correct order', () => {
    const db = openDatabase(':memory:');
    const pragmas = {
      journal_mode: db.pragma('journal_mode', { simple: true }),
      busy_timeout: db.pragma('busy_timeout', { simple: true }),
      foreign_keys: db.pragma('foreign_keys', { simple: true }),
      recursive_triggers: db.pragma('recursive_triggers', { simple: true }),
    };
    // :memory: refuses WAL silently → fall back to 'memory'. Both are valid.
    expect(['wal', 'memory']).toContain(pragmas.journal_mode);
    expect(pragmas.busy_timeout).toBe(5000);
    expect(pragmas.foreign_keys).toBe(1);
    expect(pragmas.recursive_triggers).toBe(1);
    db.close();
  });
});
```

- [ ] **Step 2 (verify Red):** `pnpm --filter @agent-brain/mcp exec vitest run src/tests/db/schema.test.ts` → fails (module not found).
- [ ] **Step 3 (Green):** Implement `db/index.ts`:

```ts
import Database, { type Database as BetterSqliteDb } from 'better-sqlite3';

export function openDatabase(path: string): BetterSqliteDb {
  const db = new Database(path);
  db.pragma('journal_mode = WAL');
  db.pragma('busy_timeout = 5000');
  db.pragma('foreign_keys = ON');
  db.pragma('recursive_triggers = ON');
  return db;
}
```

- [ ] **Step 4 (verify Green):** Test passes.
- [ ] **Step 5:** Commit: `feat(mcp/db): openDatabase with required pragmas (gotcha 10 & 11)`.

### Task 7: Migrations runner + hand-written DDL

**Files:**
- Create: `packages/mcp/src/db/ddl.ts`
- Create: `packages/mcp/src/db/migrate.ts`
- Test: extend `schema.test.ts` with a migration smoke test

- [ ] **Step 1 (Red):** Add a test:

```ts
it('applyMigrations creates all tables, view, and fts', () => {
  const db = openDatabase(':memory:');
  applyMigrations(db);
  const tables = db
    .prepare("SELECT name FROM sqlite_master WHERE type IN ('table','view') ORDER BY name")
    .all() as { name: string }[];
  const names = tables.map((t) => t.name);
  expect(names).toEqual(
    expect.arrayContaining(['projects', 'notes', 'chunks', 'embeddings', 'app_meta', 'notes_fts', 'embedding_owners']),
  );
  db.close();
});
```

- [ ] **Step 2 (verify Red):** Fails (`applyMigrations` not exported).
- [ ] **Step 3 (Green):** Implement:
  - `ddl.ts` exports a `DDL` multiline string containing the `CREATE VIRTUAL TABLE notes_fts USING fts5(...)` statement from **spec § 2.3** and the `CREATE VIEW embedding_owners AS ...` from **spec § 2.2**. Mandatory header comment: `// This file contains ONLY derived objects (virtual tables, views, triggers). // CREATE TABLE statements live in the Drizzle-generated migrations under ./migrations/. // Do not add a regular table here — it would bypass the schema source of truth.`
  - `migrate.ts` exports `applyMigrations(db)` that (a) reads `migrations/0000_init.sql` and executes it, (b) executes `DDL`.
- [ ] **Step 4 (verify Green):** Test passes.
- [ ] **Step 5:** Commit: `feat(mcp/db): migration runner with FTS5 virtual table and view`.

### Task 8: Global project seed

**Files:**
- Create: `packages/mcp/src/db/seed.ts`
- Modify: `packages/mcp/src/db/migrate.ts` to call seed after DDL
- Test: extend `schema.test.ts`

- [ ] **Step 1 (Red):** Add test:

```ts
it('seeds the global project on fresh DB', () => {
  const db = openDatabase(':memory:');
  applyMigrations(db);
  const row = db
    .prepare("SELECT id, slug, name FROM projects WHERE id = ?")
    .get('00000000-0000-0000-0000-000000000000') as { id: string; slug: string; name: string } | undefined;
  expect(row).toBeDefined();
  expect(row!.slug).toBe('global');
  expect(row!.name).toBe('Global');
});

it('seed is idempotent', () => {
  const db = openDatabase(':memory:');
  applyMigrations(db);
  applyMigrations(db);
  const count = db.prepare("SELECT COUNT(*) as c FROM projects WHERE slug='global'").get() as { c: number };
  expect(count.c).toBe(1);
});
```

- [ ] **Step 2 (Red verify):** Fails.
- [ ] **Step 3 (Green):** Implement `seed.ts` with an `INSERT OR IGNORE` using the exact literal from **spec § 2.4**. Wire it into `applyMigrations` after DDL.
- [ ] **Step 4 (Green verify):** Both tests pass.
- [ ] **Step 5:** Commit: `feat(mcp/db): seed global project idempotently`.

### Task 9: Triggers (FTS5 + embeddings cleanup)

**Files:**
- Modify: `packages/mcp/src/db/ddl.ts` (append trigger block)
- Test: `packages/mcp/src/tests/db/cascade.test.ts` (partial — asserts triggers exist)

- [ ] **Step 1 (Red):** Write a test asserting triggers exist in `sqlite_master`:

```ts
import { describe, it, expect } from 'vitest';
import { openDatabase } from '../../db/index.js';
import { applyMigrations } from '../../db/migrate.js';

describe('triggers', () => {
  it('registers the 8 expected triggers', () => {
    const db = openDatabase(':memory:');
    applyMigrations(db);
    const rows = db
      .prepare("SELECT name FROM sqlite_master WHERE type='trigger' ORDER BY name")
      .all() as { name: string }[];
    const names = rows.map((r) => r.name);
    expect(names).toEqual(
      expect.arrayContaining([
        'notes_ai_fts', 'notes_au_fts', 'notes_ad_fts',
        'chunks_ai_fts', 'chunks_au_fts', 'chunks_ad_fts',
        'notes_ad_emb', 'chunks_ad_emb',
      ]),
    );
  });
});
```

- [ ] **Step 2 (Red verify):** Fails.
- [ ] **Step 3 (Green):** Append to `ddl.ts` the 8 triggers. Use **spec § 2.3** for `notes_ai_fts` (atoms only, `WHEN NEW.kind='atom'`) and `chunks_ai_fts` (heading_path as title). Implement symmetric UPDATE/DELETE triggers. Add both cleanup triggers from **spec § 2.3**:
  - `notes_ad_emb` → `DELETE FROM embeddings WHERE owner_type='note' AND owner_id=OLD.id`
  - `chunks_ad_emb` → `DELETE FROM embeddings WHERE owner_type='chunk' AND owner_id=OLD.id`
- [ ] **Step 4 (Green verify):** Test passes.
- [ ] **Step 5:** Commit: `feat(mcp/db): FTS5 and embedding cleanup triggers`.

### Task 10: Cascade invariant tests

**Files:**
- Extend: `packages/mcp/src/tests/db/cascade.test.ts`
- Create: `packages/mcp/src/tests/_helpers/fresh-db.ts`

- [ ] **Step 1:** Create `fresh-db.ts` exporting `freshDb()` which returns `{ db, close }` with migrations applied.

```ts
import { openDatabase } from '../../db/index.js';
import { applyMigrations } from '../../db/migrate.js';

export function freshDb() {
  const db = openDatabase(':memory:');
  applyMigrations(db);
  return { db, close: () => db.close() };
}
```

- [ ] **Step 2 (Red):** Add tests (delete atom → embedding gone; delete doc → chunks + their embeddings gone; invariant no orphans; idempotent double delete).

```ts
it('deleting an atom cascades to its embedding', () => {
  const { db, close } = freshDb();
  db.prepare(`INSERT INTO notes (id, project_id, kind, title, content, tags, status, source, content_hash, created_at, updated_at)
              VALUES ('n1','00000000-0000-0000-0000-000000000000','atom','t','c','[]','active','ai','h1',1,1)`).run();
  db.prepare(`INSERT INTO embeddings (owner_type, owner_id, model, dim, content_hash, vector, created_at)
              VALUES ('note','n1','m',1,'h1', x'00', 1)`).run();
  db.prepare("DELETE FROM notes WHERE id='n1'").run();
  const remaining = db.prepare("SELECT COUNT(*) as c FROM embeddings").get() as { c: number };
  expect(remaining.c).toBe(0);
  close();
});

it('deleting a document cascades chunks and their embeddings', () => {
  const { db, close } = freshDb();
  db.prepare(`INSERT INTO notes (id, project_id, kind, title, content, tags, status, source, content_hash, created_at, updated_at)
              VALUES ('n2','00000000-0000-0000-0000-000000000000','document','t','c','[]','active','ai','h',1,1)`).run();
  db.prepare(`INSERT INTO chunks (id, note_id, position, heading_path, content, content_hash, created_at)
              VALUES ('c1','n2',0,'h','x','hc',1)`).run();
  db.prepare(`INSERT INTO embeddings (owner_type, owner_id, model, dim, content_hash, vector, created_at)
              VALUES ('chunk','c1','m',1,'hc', x'00', 1)`).run();
  db.prepare("DELETE FROM notes WHERE id='n2'").run();
  const chunks = db.prepare("SELECT COUNT(*) as c FROM chunks").get() as { c: number };
  const embs = db.prepare("SELECT COUNT(*) as c FROM embeddings").get() as { c: number };
  expect(chunks.c).toBe(0);
  expect(embs.c).toBe(0);
  close();
});

it('invariant: no orphan embeddings after any delete sequence', () => {
  const { db, close } = freshDb();
  const G = '00000000-0000-0000-0000-000000000000';
  // Seed 3 atoms
  for (let i = 0; i < 3; i++) {
    db.prepare(`INSERT INTO notes (id, project_id, kind, title, content, tags, status, source, content_hash, created_at, updated_at)
                VALUES (?, ?, 'atom', 't', 'c', '[]', 'active', 'ai', 'h', 1, 1)`).run(`a${i}`, G);
    db.prepare(`INSERT INTO embeddings (owner_type, owner_id, model, dim, content_hash, vector, created_at)
                VALUES ('note', ?, 'm', 1, 'h', x'00', 1)`).run(`a${i}`);
  }
  // Seed 1 doc with 3 chunks + 3 chunk embeddings
  db.prepare(`INSERT INTO notes (id, project_id, kind, title, content, tags, status, source, content_hash, created_at, updated_at)
              VALUES ('doc1', ?, 'document', 't', 'c', '[]', 'active', 'ai', 'h', 1, 1)`).run(G);
  for (let i = 0; i < 3; i++) {
    db.prepare(`INSERT INTO chunks (id, note_id, position, heading_path, content, content_hash, created_at)
                VALUES (?, 'doc1', ?, 'h', 'x', 'ch', 1)`).run(`c${i}`, i);
    db.prepare(`INSERT INTO embeddings (owner_type, owner_id, model, dim, content_hash, vector, created_at)
                VALUES ('chunk', ?, 'm', 1, 'ch', x'00', 1)`).run(`c${i}`);
  }
  // Delete 1 atom and the doc (cascade triggers chunks + their embeddings)
  db.prepare(`DELETE FROM notes WHERE id='a0'`).run();
  db.prepare(`DELETE FROM notes WHERE id='doc1'`).run();
  const orphans = db.prepare(`
    SELECT COUNT(*) AS c FROM embeddings
    WHERE NOT (
      (owner_type='note'  AND owner_id IN (SELECT id FROM notes)) OR
      (owner_type='chunk' AND owner_id IN (SELECT id FROM chunks))
    )
  `).get() as { c: number };
  expect(orphans.c).toBe(0);
  // Remaining: 2 atoms (a1, a2) = 2 embeddings, no chunks left.
  const total = db.prepare(`SELECT COUNT(*) AS c FROM embeddings`).get() as { c: number };
  expect(total.c).toBe(2);
  close();
});
```

- [ ] **Step 3 (Red verify):** Tests run.
- [ ] **Step 4 (Green):** All 3 tests pass given Task 9's triggers.
- [ ] **Step 5:** Commit: `test(mcp/db): cascade invariants for triggers`.

### Task 11: Backup helper + test

**Files:**
- Create: `packages/mcp/src/db/backup.ts`
- Create: `packages/mcp/src/db/hash.ts`
- Test: `packages/mcp/src/tests/db/backup.test.ts`

- [ ] **Step 1:** Create `hash.ts`: `export const sha256 = (s: string): string => crypto.createHash('sha256').update(s).digest('hex');`
- [ ] **Step 2 (Red):** Write test that opens a file-backed DB in a temp dir, inserts a note, calls `checkpointAndCopy()`, verifies the backup file exists, opens the backup, and reads the note back.
- [ ] **Step 3 (Red verify):** Fails.
- [ ] **Step 4 (Green):** Implement `backup.ts`:

```ts
import { copyFileSync } from 'node:fs';
import type { Database } from 'better-sqlite3';

export function checkpointAndCopy(db: Database, destination: string): number {
  db.pragma('wal_checkpoint(TRUNCATE)');
  copyFileSync(db.name, destination);
  const { size } = require('node:fs').statSync(destination);
  return size as number;
}
```

- [ ] **Step 5 (Green verify):** Test passes.
- [ ] **Step 6:** Commit: `feat(mcp/db): checkpoint-safe backup helper (gotcha 4)`.

### Task 12: Mock-embeddings helper + vi.mock setup

**Files:**
- Create: `packages/mcp/src/tests/setup.ts`
- Create: `packages/mcp/src/tests/_helpers/mock-embeddings.ts`

- [ ] **Step 1:** Implement `mock-embeddings.ts` exactly as in **spec § 6.1.1** (sha256 seed → 768 floats → L2 normalize).
- [ ] **Step 2:** Implement `setup.ts`:

```ts
import { vi } from 'vitest';
import { mockEmbed } from './_helpers/mock-embeddings.js';

vi.mock('@huggingface/transformers', () => {
  const pipeline = vi.fn(async () => {
    const fn = async (text: string) => ({ data: mockEmbed(text) });
    return Object.assign(fn, {
      tokenizer: {
        encode: (t: string) => ({ length: Math.ceil(t.length / 4) }),
      },
    });
  });
  const AutoTokenizer = {
    from_pretrained: vi.fn(async () => ({
      encode: (t: string) => ({ length: Math.ceil(t.length / 4) }),
    })),
  };
  return { pipeline, AutoTokenizer };
});
```

- [ ] **Step 3:** Add a smoke test `mock-embeddings.test.ts` that asserts `mockEmbed('x').length === 768`, `mockEmbed('x')` deterministic, and `dot(mockEmbed('a'), mockEmbed('a')) ≈ 1`.
- [ ] **Step 4:** Run tests — all pass, no model download.
- [ ] **Step 5:** Commit: `test(mcp): embedding mock + global vi.mock setup`.

### Task 13: Test factories

**Files:**
- Create: `packages/mcp/src/tests/_helpers/factories.ts`

- [ ] **Step 1:** Implement builder functions `makeProject(overrides)`, `makeNote(overrides)`, `makeChunk(overrides)` that return well-formed DB-shaped objects with sane defaults + a deterministic UUID counter. No test new in this task — used by subsequent tasks.
- [ ] **Step 2:** Commit: `test(mcp): factory helpers for DB fixtures`.

### Task 14: Embeddings pipeline wrapper

**Files:**
- Create: `packages/mcp/src/embeddings/pipeline.ts`
- Create: `packages/mcp/src/embeddings/embed.ts`
- Test: `packages/mcp/src/tests/embeddings/embed.test.ts`

- [ ] **Step 1 (Red):** Test:

```ts
import { describe, it, expect } from 'vitest';
import { embedPassage, embedQuery } from '../../embeddings/embed.js';

describe('embed', () => {
  it('embedPassage returns a 768-d L2-normalized vector', async () => {
    const v = await embedPassage('hello');
    expect(v).toBeInstanceOf(Float32Array);
    expect(v.length).toBe(768);
    const norm = Math.sqrt(v.reduce((a, x) => a + x * x, 0));
    expect(norm).toBeCloseTo(1, 5);
  });
  it('embedPassage and embedQuery produce different vectors for the same text', async () => {
    const p = await embedPassage('hello');
    const q = await embedQuery('hello');
    // passage: vs query: prefixes → different inputs → different mock outputs
    let dot = 0;
    for (let i = 0; i < 768; i++) dot += p[i]! * q[i]!;
    expect(dot).toBeLessThan(0.999);
  });
});
```

- [ ] **Step 2 (Red verify):** Fails.
- [ ] **Step 3 (Green):**
  - `pipeline.ts`: `let cached: Promise<unknown> | null = null; export async function getEmbeddingPipeline() { if (!cached) { cached = (async () => { const { pipeline } = await import('@huggingface/transformers'); // @ts-expect-error feature-extraction pipeline has a loose union return type (gotcha 5) ; return await pipeline('feature-extraction', 'Xenova/multilingual-e5-base'); })(); } return cached; }` — typed cache promise, `@ts-expect-error` documented for gotcha 5.
  - `embed.ts`: exports `embedPassage(text)` and `embedQuery(text)`, both call the pipeline with the correct prefix (**spec § 4.3**), `pooling: 'mean'`, `normalize: true`.
- [ ] **Step 4 (Green verify):** Tests pass.
- [ ] **Step 5:** Commit: `feat(mcp/embeddings): E5 passage/query wrappers (gotcha 8)`.

### Task 15: E5 tokenizer wrapper

**Files:**
- Create: `packages/mcp/src/embeddings/tokenizer.ts`
- Test: extend `embed.test.ts`

- [ ] **Step 1 (Red):** Test `countTokens('hello world')` returns a number > 0 and `<= 450` for short strings.
- [ ] **Step 2 (Green):** Implement:

```ts
let cached: any;
export async function getTokenizer() {
  if (!cached) {
    const { AutoTokenizer } = await import('@huggingface/transformers');
    cached = await AutoTokenizer.from_pretrained('Xenova/multilingual-e5-base');
  }
  return cached;
}
export async function countTokens(text: string): Promise<number> {
  const tok = await getTokenizer();
  const encoded = tok.encode('passage: ' + text);
  return encoded.length ?? encoded.input_ids?.length ?? 0;
}
```

- [ ] **Step 3:** Test passes (mock returns length from setup).
- [ ] **Step 4:** Commit: `feat(mcp/embeddings): E5 tokenizer wrapper (gotcha 9 & 12)`.

### Task 16: AST chunker — happy path

**Files:**
- Create: `packages/mcp/src/embeddings/chunking.ts`
- Create: `packages/mcp/src/config.ts` (exports `CHUNK_CONFIG` from **spec § 4.2**)
- Test: `packages/mcp/src/tests/embeddings/chunking.test.ts`

- [ ] **Step 1:** Create `config.ts` with `CHUNK_CONFIG` literal.
- [ ] **Step 2 (Red):** Test:

```ts
it('splits a simple doc by H2 headers', async () => {
  const md = `# Title\n\n## Alpha\n\nAlpha body.\n\n## Beta\n\nBeta body.`;
  const chunks = await chunk(md);
  expect(chunks).toHaveLength(3); // Title intro + Alpha + Beta
  expect(chunks[0]!.heading_path).toBe('Title');
  expect(chunks[1]!.heading_path).toBe('Title > Alpha');
  expect(chunks[2]!.heading_path).toBe('Title > Beta');
  chunks.forEach((c, i) => expect(c.position).toBe(i));
});
```

- [ ] **Step 3 (Red verify):** Fails.
- [ ] **Step 4 (Green):** Implement `chunk(markdown: string): Promise<Chunk[]>` using `unified().use(remarkParse).use(remarkGfm).parse(md)`, then walk the AST with `unist-util-visit`, maintaining a header stack (depth ≤ 3). Each split point flushes the current chunk. Attach `position` (0-indexed), `heading_path` (stack joined with ` > `), `content` (markdown serialization of the chunk's nodes via a tiny stringifier, or a position-slice fallback), `content_hash` (sha256).
- [ ] **Step 5 (Green verify):** Test passes.
- [ ] **Step 6:** Commit: `feat(mcp/chunking): AST-based header split (gotcha 14)`.

### Task 17: AST chunker — fenced code & round-trip invariant

**Files:**
- Extend: `chunking.test.ts`

- [ ] **Step 1 (Red):** Tests:

```ts
it('does not split on # inside fenced code blocks', async () => {
  const md = [
    '## Real Header',
    '',
    '```sql',
    'CREATE TABLE t (',
    '  -- # this comment starts with #',
    '  id INTEGER',
    ');',
    '## also not a header inside code',
    '```',
    '',
    '## Another Real Header',
    '',
    'body',
  ].join('\n');
  const chunks = await chunk(md);
  expect(chunks).toHaveLength(2);
  expect(chunks[0]!.heading_path).toBe('Real Header');
  expect(chunks[1]!.heading_path).toBe('Another Real Header');
});

it('invariant: joining chunk contents reproduces the original (whitespace-normalized equality)', async () => {
  const md = `# A\n\n## B\n\nparagraph\n\n\`\`\`js\nconst x = 1;\n\`\`\`\n\n## C\n\nmore`;
  const chunks = await chunk(md);
  const rejoined = chunks.map((c) => c.content).join('\n\n');
  // Strong invariant: the full original content is present after normalization.
  // Using equality (not containment) is critical — containment on substrings gives false-greens
  // when a regression silently drops a section.
  const normalize = (s: string) => s.replace(/\s+/g, ' ').trim();
  expect(normalize(rejoined)).toBe(normalize(md));
});
```

- [ ] **Step 2 (verify):** Tests pass if Task 16's AST implementation is correct. If they fail (regex fallback), fix chunker to walk nodes and serialize via `mdast-util-to-markdown`.
- [ ] **Step 3:** Commit: `test(mcp/chunking): fenced code safety and round-trip invariant`.

### Task 18: Paragraph + sentence fallback for oversized sections

**Files:**
- Modify: `chunking.ts`
- Extend: `chunking.test.ts`

- [ ] **Step 1 (Red):** Test that a 600-token section (mock counts chars/4) is split into ≥2 chunks all ≤ 450 tokens. Use a 2500-char paragraph-rich section.
- [ ] **Step 2 (Red verify):** Fails (current impl emits 1 chunk).
- [ ] **Step 3 (Green):** In `chunking.ts`, after assembling a section chunk, if `countTokens(content) > CHUNK_CONFIG.maxTokens`, split by `\n\n`, greedy-pack into sub-chunks ≤ 450. If any single paragraph > 450, split by sentence regex `/(?<=[.!?])\s+/` and log `console.warn('paragraph exceeds ...')`. All sub-chunks share the same `heading_path`.
- [ ] **Step 4 (Green verify):** Test passes. Add a 3rd test that fuzz-generates 20 random markdown docs and asserts all resulting chunks satisfy `countTokens(c.content) <= 450`.
- [ ] **Step 5:** Commit: `feat(mcp/chunking): paragraph and sentence fallback for oversized sections`.

### Task 18b: Real-E5 smoke test for chunker (opt-in, covers gotcha 12)

> **Why separate**: all chunking tests above use the mocked tokenizer (`Math.ceil(t.length / 4)`), which is a char-count, not a true E5 token count. Gotcha 12 is only really validated if we exercise the chunker against the real `Xenova/multilingual-e5-base` tokenizer at least once before a merge. This test is **opt-in** (skipped in CI, run locally on demand) to keep the default suite fast and offline.

**Files:**
- Create: `packages/mcp/src/tests/embeddings/chunking-real-e5.test.ts`
- Modify: `packages/mcp/package.json` add `"test:e5": "vitest run src/tests/embeddings/chunking-real-e5.test.ts"` script.

- [ ] **Step 1:** Write the test:

```ts
import { describe, it, expect, vi } from 'vitest';
import { chunk } from '../../embeddings/chunking.js';

// Unmock the HF transformers module so this file uses the real E5 tokenizer.
vi.unmock('@huggingface/transformers');

const runInCi = process.env.CI === 'true';

describe.skipIf(runInCi)('chunker — real E5 tokenizer', () => {
  it('every chunk is <= 450 tokens under the real E5 tokenizer', async () => {
    // French-heavy sample — representative of real agent-brain content.
    const md = [
      '# ADN Barda',
      '',
      '## Section 1 — Stack',
      'Node.js 20, TypeScript strict, Drizzle, FTS5. '.repeat(40),
      '',
      '## Section 2 — Embeddings',
      'Le modèle multilingue E5-base encode chaque passage avec un préfixe obligatoire. '.repeat(50),
      '',
      '## Section 3 — Chunking',
      'Paragraphe 1 sur le chunking.\n\nParagraphe 2 sur le chunking.\n\nParagraphe 3.'.repeat(10),
    ].join('\n');

    const chunks = await chunk(md);
    const { AutoTokenizer } = await import('@huggingface/transformers');
    const tokenizer = await AutoTokenizer.from_pretrained('Xenova/multilingual-e5-base');

    for (const c of chunks) {
      const encoded = tokenizer.encode('passage: ' + c.content);
      const len = (encoded as any).length ?? (encoded as any).input_ids?.length ?? 0;
      expect(len, `chunk at position ${c.position} (${c.heading_path})`).toBeLessThanOrEqual(450);
    }
  }, 30_000); // 30s timeout — first run downloads the ~200MB model.
});
```

- [ ] **Step 2:** Add the `test:e5` script to `packages/mcp/package.json`.
- [ ] **Step 3:** Run `pnpm --filter @agent-brain/mcp test:e5` locally once to prove it works (first run downloads the model ~5-10s; subsequent runs reuse the HF cache).
- [ ] **Step 4:** Add a pre-merge reminder in `CLAUDE.md`: "Before merging any PR that touches `src/embeddings/chunking.ts` or `src/embeddings/tokenizer.ts`, run `pnpm --filter @agent-brain/mcp test:e5` locally."
- [ ] **Step 5:** Commit: `test(mcp/chunking): real-E5 smoke test (gotcha 12 guard)`.

### Task 19: `project_*` tools (4) + tests

**Files:**
- Create: `packages/mcp/src/tools/projects.ts`
- Test: `packages/mcp/src/tests/tools/projects.test.ts`

- [ ] **Step 1 (Red):** Write tests covering the 4 tools against a `freshDb()`:
  - `project_create('cruchot', 'Cruchot')` → returns `{ ok, id, item }`, row exists in DB.
  - Duplicate slug → Zod/sql error surfaced.
  - `project_list()` includes `global` + created projects.
  - `project_update` patches fields.
  - `project_delete` refuses `GLOBAL_PROJECT_ID` (Zod-level `.refine`).
  - `project_delete` on a non-global project reassigns any notes to global and returns `reassigned_count`.
- [ ] **Step 2 (Red verify):** Fails.
- [ ] **Step 3 (Green):** Implement handlers. Each handler is a pure function `(db, input) => output`. Use Drizzle query builder. Use `crypto.randomUUID()` and `Date.now()`. Validate input with Zod schemas declared inline (spec § 3.2). Each handler is typed to return the appropriate response envelope from `@agent-brain/shared`.
- [ ] **Step 4 (Green verify):** All 6 tests pass.
- [ ] **Step 5:** Commit: `feat(mcp/tools): project CRUD with global guard`.

### Task 20: Contract test framework

**Files:**
- Create: `packages/mcp/src/tools/contracts.ts`
- Create: `packages/mcp/src/tests/tools/contracts.test.ts`

- [ ] **Step 1:** `contracts.ts` re-exports response schemas from `@agent-brain/shared` and defines a map `RESPONSE_SHAPES: Record<ToolName, ZodSchema>`. Example: `{ project_create: MutationResponseSchema(ProjectSchema), project_list: ListResponseSchema(ProjectSchema), ... }`.
- [ ] **Step 2:** `contracts.test.ts` runs each of the 4 project tools, calls `.parse()` on their result against the corresponding shape — any drift explodes the test.
- [ ] **Step 3:** All tests pass.
- [ ] **Step 4:** Commit: `test(mcp/tools): runtime contract validation (gotcha 2)`.

### Task 21: `note_create` (atom path) + test

**Files:**
- Create: `packages/mcp/src/tools/notes.ts`
- Test: `packages/mcp/src/tests/tools/notes.test.ts`

- [ ] **Step 1 (Red):** Test:
  - `note_create({ kind: 'atom', title, content, tags: ['gotcha'], source: 'ai' })` returns `{ ok, id, item }`, row exists in `notes`, 1 row in `embeddings` with `owner_type='note'`, `content_hash` matches sha256(content), tags stored as JSON string `["gotcha"]`, FTS5 row present.
- [ ] **Step 2 (Red verify):** Fails.
- [ ] **Step 3 (Green):** Implement `note_create` for `kind='atom'` only (guarded; `document` throws `not implemented` to be completed in Task 22). Handler flow:
  1. Parse input with Zod.
  2. Compute `content_hash = sha256(content)`.
  3. Generate id, now = Date.now().
  4. Begin transaction.
  5. INSERT into `notes` with `tags: JSON.stringify(input.tags)`.
  6. `vec = await embedPassage(input.content)`.
  7. INSERT into `embeddings` with `owner_type='note'`, `owner_id=id`, `model='Xenova/multilingual-e5-base'`, `dim=768`, vector as `Buffer.from(vec.buffer, vec.byteOffset, vec.byteLength)`.
  8. Commit tx.
  9. Return `{ ok: true, id, item: hydrated }`.
- [ ] **Step 4 (Green verify):** Test passes.
- [ ] **Step 5:** Commit: `feat(mcp/tools): note_create atom path with embedding`.

### Task 22: `note_create` (document path) + test

**Files:**
- Modify: `packages/mcp/src/tools/notes.ts`
- Extend: `packages/mcp/src/tests/tools/notes.test.ts`

- [ ] **Step 1 (Red):** Test: `note_create({ kind: 'document', content: multiSectionMarkdown, ... })` → verifies (a) `notes` row exists, (b) `chunks` has N rows with distinct positions, (c) `embeddings` has N rows all `owner_type='chunk'`, zero on `owner_type='note'`.
- [ ] **Step 2 (Red verify):** Fails with `not implemented`.
- [ ] **Step 3 (Green):** Extend handler: when `kind='document'`, after inserting the note, call `chunks = await chunk(content)`, loop and insert each chunk row + one embedding row per chunk (via `embedPassage(chunk.content)`). Wrap in a transaction.
- [ ] **Step 4 (Green verify):** Test passes.
- [ ] **Step 5:** Commit: `feat(mcp/tools): note_create document path with chunking + embeddings`.

### Task 23: Diff-based sync-chunks

**Files:**
- Create: `packages/mcp/src/notes/sync-chunks.ts`
- Test: `packages/mcp/src/tests/notes/sync-chunks.test.ts`

- [ ] **Step 1 (Red):** Tests from **spec § 6.1** suite `sync-chunks.test.ts`:
  1. Unchanged section kept (same id, same embedding).
  2. Edit in 1 section → exactly 1 embed regenerated.
  3. Add section at end → 1 new chunk, others untouched (ids preserved).
  4. Add section in middle → 1 new chunk, positions of following updated, 0 re-embed.
  5. Delete a section → that chunk + its embedding gone, positions updated.
  6. Rename heading only → 1 `heading_path` UPDATE, 0 re-embed.
  7. Invariant final: `count(chunks where note_id=X) == count(embeddings where owner_type='chunk' and owner_id in ...)`.
- [ ] **Step 2 (Red verify):** Fails (module missing).
- [ ] **Step 3 (Green):** Implement `syncDocumentChunks(db, noteId, newContent)` exactly as in **spec § 3.3.1**. Transaction required.
- [ ] **Step 4 (Green verify):** All tests pass.
- [ ] **Step 5:** Commit: `feat(mcp/notes): diff-based chunk sync with hash reuse`.

### Task 24: `note_get`, `note_delete`, `note_list`

**Files:**
- Modify: `packages/mcp/src/tools/notes.ts`
- Extend: `packages/mcp/src/tests/tools/notes.test.ts`

- [ ] **Step 1 (Red):** Tests for each tool:
  - `note_get(id)` → returns `{ item }` with `chunks` populated for documents, absent for atoms.
  - `note_delete(id)` → row gone, trigger cascade verified (use `cascade.test.ts` helpers).
  - `note_list` filters: `project_id`, `kind`, `status`, `tags_any`, `tags_all` (refine error when both), `source`, `sort`, `limit`, `offset`.
- [ ] **Step 2 (Green):** Implement. For `note_list` tags, use `json_each()` in SQL — no JS post-filter. Mutual exclusion enforced by Zod `.refine`.
- [ ] **Step 3:** Tests pass.
- [ ] **Step 4:** Commit: `feat(mcp/tools): note_get, note_delete, note_list`.

### Task 25: `note_update` wired to sync-chunks

**Files:**
- Modify: `packages/mcp/src/tools/notes.ts`
- Extend: `packages/mcp/src/tests/tools/notes.test.ts`

- [ ] **Step 1 (Red):** Tests:
  - Patch `title` only → no re-chunk, no re-embed. Assert by counting calls to a spy on `embedPassage`.
  - Patch `content` atom → 1 re-embed.
  - Patch `content` document → delegates to `syncDocumentChunks`, assert behavior matches Task 23.
  - Patch `content` unchanged (same hash) → 0 re-embed.
- [ ] **Step 2 (Green):** Implement using the skip-if-unchanged logic from **spec § 3.3**.
- [ ] **Step 3:** Commit: `feat(mcp/tools): note_update with skip-if-hash-unchanged`.

### Task 26: Serialization round-trip test

**Files:**
- Test: `packages/mcp/src/tests/db/serialization.test.ts`

- [ ] **Step 1:** Tests: `tags: string[]` round-trip via `JSON.stringify` on write and `JSON.parse` on read. `NoteSchema` validates the hydrated object. Rejects malformed JSON row stored manually.
- [ ] **Step 2:** Commit: `test(mcp/db): notes.tags JSON serialization round-trip`.

### Task 27: Fulltext search

**Files:**
- Create: `packages/mcp/src/search/fulltext.ts`
- Test: `packages/mcp/src/tests/search/fulltext.test.ts` (subset of `hybrid.test.ts`)

- [ ] **Step 1 (Red):** Test a seeded corpus with 5 notes, query matches by title vs content, filter by `project_id`, by `tags_any` (via `json_each`), verify hits limited to `limit`, snippet returned via `snippet(notes_fts, ...)`.
- [ ] **Step 2 (Green):** Implement `fulltextSearch(db, input)` using a single SQL with `MATCH`, `JOIN notes n` for filtering, `json_each` for tags, `bm25(notes_fts)` for scoring, `snippet(...)` for highlight. Filter-before-ranking in WHERE.
- [ ] **Step 3:** Commit: `feat(mcp/search): fulltext FTS5 with filter-before-ranking`.

### Task 28: Semantic search

**Files:**
- Create: `packages/mcp/src/search/semantic.ts`
- Test: extend `hybrid.test.ts`

- [ ] **Step 1 (Red):** Test semantic ranking orders hits by cosine similarity to query embedding. Filter-before-ranking via the view `embedding_owners`.
- [ ] **Step 2 (Green):** Implement `semanticSearch(db, input)`:
  1. `queryVec = await embedQuery(input.query)`.
  2. Load `rows = db.prepare('SELECT owner_type, owner_id, vector, project_id, kind, status, tags, source FROM embedding_owners WHERE model=? AND [filters]').all(...)`.
  3. For each row, parse `Float32Array(new Uint8Array(row.vector).buffer)`, compute dot product (vectors are L2-normalized → dot == cosine), push `{ owner_type, owner_id, score }`.
  4. Sort desc, take `limit`.
- [ ] **Step 3:** Tests pass.
- [ ] **Step 4:** Commit: `feat(mcp/search): semantic search via view + cosine subset`.

### Task 29: Hybrid RRF + neighbor expansion

**Files:**
- Create: `packages/mcp/src/search/hybrid.ts`
- Create: `packages/mcp/src/search/neighbors.ts`
- Test: extend `hybrid.test.ts`

- [ ] **Step 1 (Red):** Tests from **spec § 6.1**: RRF (k=60) combines fulltext + semantic hits, a hit top-1 in both ranks above a hit top-1 in only one. Neighbor expansion on chunk hits loads `position ± 1..N` content truncated to 300 chars.
- [ ] **Step 2 (Green):**
  - `hybrid.ts`: `hybridSearch(db, input)` calls `fulltextSearch` and `semanticSearch`, merges hits by `owner_id`, computes `score = Σ 1/(60 + rank_i)`, sorts, trims to `limit`.
  - `neighbors.ts`: `expandNeighbors(db, hits, n)` — for chunk hits, loads siblings from same `note_id` where `|position - hit.position| <= n` and truncates content.
- [ ] **Step 3:** Commit: `feat(mcp/search): RRF hybrid + neighbor expansion`.

### Task 30: `search` tool + full hit hydration

**Files:**
- Create: `packages/mcp/src/tools/search.ts`
- Test: `packages/mcp/src/tests/tools/search.test.ts`

- [ ] **Step 1 (Red):** Tests:
  - `mode` switch dispatches correctly.
  - `tags_all` + `tags_any` mutually exclusive (Zod refine).
  - Hit hydration: each hit has `parent` populated (`title`, `kind`, `project_id`, `tags[]`, `status`) via a JOIN lookup on `notes`.
  - For chunk hits, `chunk.heading_path` and `chunk.position` populated.
- [ ] **Step 2 (Green):** Implement. Delegate to the 3 search functions, then hydrate via a single `SELECT ... FROM notes WHERE id IN (...)` query.
- [ ] **Step 3:** Commit: `feat(mcp/tools): unified search tool with hydration`.

### Task 31: `backup_brain` + `get_stats`

**Files:**
- Create: `packages/mcp/src/tools/maintenance.ts`
- Test: `packages/mcp/src/tests/tools/maintenance.test.ts`

- [ ] **Step 1 (Red):** Tests:
  - `backup_brain({ label: 'snapshot' })` creates a file at `~/.agent-brain/backups/<ts>-snapshot.db` (use a tmpdir override for tests), returns `{ ok, path, size_bytes }`.
  - `get_stats()` returns `Stats` shape matching `StatsSchema`, with correct counts after seeding a fresh DB.
  - `get_stats({ project_id })` scoped stats.
- [ ] **Step 2 (Green):** Implement. For tests, inject a backup dir path (the handler accepts `options.backupDir` defaulting to `~/.agent-brain/backups/`).
- [ ] **Step 3:** Commit: `feat(mcp/tools): backup_brain and get_stats`.

### Task 32: MCP stdio server wiring

**Files:**
- Create: `packages/mcp/src/tools/index.ts`
- Modify: `packages/mcp/src/index.ts`
- Test: `packages/mcp/src/tests/tools/register.test.ts`

- [ ] **Step 1:** `tools/index.ts` exports `registerAllTools(server, db)` which calls `server.tool(name, description, zodSchema, handler)` for all 12 tools. Each handler wraps the pure function with a DB-bound closure and returns `{ content: [{ type: 'text', text: JSON.stringify(result) }] }` per MCP convention. **Add a comment** above any `server._registeredTools` access: `// _registeredTools is a private SDK surface (underscore prefix). Brittle to @modelcontextprotocol/sdk upgrades — re-verify this field still exists on every bump.`
- [ ] **Step 2:** `index.ts` main:

```ts
#!/usr/bin/env node
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
import { openDatabase } from './db/index.js';
import { applyMigrations } from './db/migrate.js';
import { registerAllTools } from './tools/index.js';
import { resolve } from 'node:path';
import { homedir } from 'node:os';

const argv = process.argv.slice(2);
const useUi = argv.includes('--ui');
const dbFlag = argv.indexOf('--db-path');
const dbPath = dbFlag >= 0 ? argv[dbFlag + 1]! : resolve(homedir(), '.agent-brain', 'brain.db');

const db = openDatabase(dbPath);
applyMigrations(db);
const server = new McpServer({ name: 'agent-brain', version: '0.1.0' });
registerAllTools(server, db);

if (useUi) {
  const { startHttpServer } = await import('./http/server.js');
  await startHttpServer(server, db);
} else {
  const transport = new StdioServerTransport();
  await server.connect(transport);
}
```

- [ ] **Step 3:** Test: `register.test.ts` boots a server instance, asserts 12 tool names are present in `server._registeredTools`.
- [ ] **Step 4:** Commit: `feat(mcp): stdio server wiring with tool registry`.

### Task 33: Express HTTP + JSON-RPC bridge

**Files:**
- Create: `packages/mcp/src/http/server.ts`
- Create: `packages/mcp/src/http/jsonrpc.ts`
- Test: `packages/mcp/src/tests/http/jsonrpc.test.ts`

- [ ] **Step 1 (Red):** Test uses `supertest` or raw `fetch` against `startHttpServer` on an ephemeral port. POST `/mcp` with JSON-RPC body `{ jsonrpc: '2.0', id: 1, method: 'project_list', params: {} }` → 200 with `{ jsonrpc: '2.0', id: 1, result: { items, total, limit, offset } }`.
- [ ] **Step 2 (Red verify):** Fails.
- [ ] **Step 3 (Green):** Implement:
  - `jsonrpc.ts`: `handleJsonRpc(server, body)` — lookup method in `server._registeredTools`, call handler, wrap result in JSON-RPC envelope, handle errors → `{ error: { code: -32000, message } }`.
  - `server.ts`: Express 5 app with `app.use(express.json())`, `app.post('/mcp', ...)`, `app.get('/health', ...)` (Task 34), `app.get('/{*path}', ...)` to serve `packages/mcp/public/index.html` (stub for now). `startHttpServer` binds `127.0.0.1` on a port from env or 4000.
- [ ] **Step 4 (Green verify):** Test passes.
- [ ] **Step 5:** Commit: `feat(mcp/http): Express 5 + JSON-RPC bridge (gotcha 1)`.

### Task 34: `/health`, embedder preload, `--ui` flow

**Files:**
- Create: `packages/mcp/src/http/health.ts`
- Modify: `packages/mcp/src/http/server.ts`
- Test: `packages/mcp/src/tests/http/health.test.ts`

- [ ] **Step 1 (Red):** Test `GET /health` returns `{ embedder_ready: boolean }`, starts `false`, flips `true` after the preload fire-and-forget resolves (await a short `setTimeout` or spy).
- [ ] **Step 2 (Green):** Implement a module-level boolean `embedderReady`, set to `true` in a `.then(() => ...)` after `getEmbeddingPipeline()` resolves. Kick off the promise **after** `app.listen()` in `startHttpServer`.
- [ ] **Step 3:** Commit: `feat(mcp/http): /health endpoint and embedder preload (gotcha 13)`.

### Task 35: `app_meta` + CHUNK_CONFIG version guard

**Files:**
- Create: `packages/mcp/src/db/app-meta.ts`
- Modify: `packages/mcp/src/db/migrate.ts`
- Test: `packages/mcp/src/tests/db/app-meta.test.ts`

- [ ] **Step 1 (Red):** Test:
  - Fresh DB → `getMeta(db, 'chunk_config_version') === '1'` after migrations.
  - Bump `CHUNK_CONFIG.version` to `2` in test → `checkChunkConfigVersion(db)` logs a warning (spy on `console.warn`) but does not throw.
- [ ] **Step 2 (Green):** `app-meta.ts` exports:
  - `getMeta(db, key): string | undefined`
  - `setMeta(db, key, value): void`
  - `checkChunkConfigVersion(db): void` — reads `meta['chunk_config_version']`, compares to `CHUNK_CONFIG.version`, and if different logs `console.warn('chunk config changed from <old> to <new>, run pnpm mcp:reindex')`. **Does not** update the stored value.
  - `markChunkConfigReindexed(db, version): void` — called at the **end** of the reindex CLI script (Task 36) after all documents have been re-chunked with the current config. Updates `meta['chunk_config_version']` to the passed version.
  - On fresh DB (empty `app_meta`), `checkChunkConfigVersion` initializes `meta['chunk_config_version']` to `String(CHUNK_CONFIG.version)` so the warning never fires on a brand-new install.
- [ ] **Step 3:** Commit: `feat(mcp/db): app_meta table + chunk config version guard with mark-reindexed API`.

### Task 36: CLI scripts `mcp:restore` & `mcp:reindex`

**Files:**
- Create: `packages/mcp/src/scripts/restore.ts`
- Create: `packages/mcp/src/scripts/reindex.ts`
- Test: `packages/mcp/src/tests/scripts/restore.test.ts`

- [ ] **Step 1 (Red):** Test:
  - `restore(path)` with a lock file present throws `restore already in progress`.
  - Normal path: closes DB, copies source over `brain.db`, cleans `.db-wal` and `.db-shm`, removes lock, reopens.
- [ ] **Step 2 (Green):** Implement both scripts. `restore.ts` exports a `main(argv)` and has a `if (import.meta.url === …)` guard. `reindex.ts` walks all documents, re-runs `syncDocumentChunks` with current config, then calls `markChunkConfigReindexed(db, CHUNK_CONFIG.version)` from Task 35.
- [ ] **Step 3:** Also add a guard at the top of `openDatabase` that refuses to open if `~/.agent-brain/restore.lock` exists.
- [ ] **Step 4:** Commit: `feat(mcp/scripts): restore and reindex CLI scripts, mark reindex complete`.

### Task 37: tsup build + bin + smoke test

**Files:**
- Modify: `packages/mcp/tsup.config.ts`
- Modify: `packages/mcp/package.json` (`bin`)

- [ ] **Step 1:** Add `"bin": { "agent-brain": "./dist/index.js" }` to `packages/mcp/package.json`.
- [ ] **Step 2:** `pnpm --filter @agent-brain/mcp build` → emits `dist/index.js`, `dist/scripts/restore.js`, `dist/scripts/reindex.js` with shebang on `index.js`.
- [ ] **Step 3:** Smoke:

```bash
node packages/mcp/dist/index.js --db-path /tmp/brain-smoke.db --ui &
sleep 1
curl -s http://127.0.0.1:4000/health   # expect { embedder_ready: false }
sleep 10
curl -s http://127.0.0.1:4000/health   # expect { embedder_ready: true }
kill %1   # critical: never leave a zombie node process
```
- [ ] **Step 4:** Commit: `chore(mcp): tsup build config and smoke test verified`.

### Task 38: README, Claude Code config, CLAUDE.md

**Files:**
- Create: `README.md`
- Create: `CLAUDE.md`

- [ ] **Step 1:** Write `README.md` covering: install (`pnpm install`, `pnpm build`), dev mode (`pnpm dev`), Claude Code stdio config snippet from **spec § 6.2**, DB path, backup/restore scripts, where to find the spec.
- [ ] **Step 2:** Write `CLAUDE.md` at repo root that references `docs/superpowers/specs/...` and inlines the 14-gotcha table from **spec § 6.3**.
- [ ] **Step 3:** Commit: `docs: README and CLAUDE.md referencing spec and gotchas`.

---

## Self-review (inline)

**Spec coverage check:**
- § 1.1 two modes → Tasks 32, 33, 34 ✅
- § 1.2 DB path + pragmas → Task 6 ✅
- § 2.1 schema → Task 5 ✅
- § 2.2 view → Task 7 ✅
- § 2.3 FTS5 + triggers → Tasks 7, 9 ✅
- § 2.4 seed → Task 8 ✅
- § 2.5 content_hash at 3 levels → Tasks 21, 22, 23 ✅
- § 3.1.1 Zod response schemas → Task 3 (shared) + Task 20 (contracts test) ✅
- § 3.2 project tools → Task 19 ✅
- § 3.3 note tools → Tasks 21, 22, 24, 25 ✅
- § 3.3.1 diff-based sync → Task 23 ✅
- § 3.4 search (3 modes + RRF + neighbors + filter-before-ranking + source + tags WHERE) → Tasks 27, 28, 29, 30 ✅
- § 3.5 backup/stats/restore CLI → Tasks 31, 36 ✅
- § 4.1 AST chunker → Tasks 16, 17, 18, **18b (real-E5 gotcha 12 guard)** ✅
- § 4.2 CHUNK_CONFIG + version guard → Tasks 16, 35 ✅
- § 4.3 E5 prefixes → Task 14 ✅
- § 4.4 preload → Task 34 ✅
- § 5 UI → **out of scope, Plan B**
- § 6.1 tests (all suites) → Tasks 10, 17, 18, 19, 20, 23, 26, 27-30, 31 ✅
- § 6.1.1 mock-embeddings → Task 12 ✅
- § 6.2 build & packaging → Tasks 4, 37, 38 ✅
- § 6.3 14 gotchas → referenced across tasks, README in Task 38 ✅

**Placeholder scan:** No `TBD`, `TODO`, or "handle edge cases" phrasing. Tasks 4 and 19+ reference spec sections for exact Zod input schemas (not forbidden: the spec is versioned and checked-in). Every code step shows concrete code or an exact command.

**Type consistency:** `syncDocumentChunks` signature stable across Tasks 23, 25, 36. `embedPassage`/`embedQuery` stable across Tasks 14, 21, 22, 23, 25. `openDatabase` signature stable across all DB tasks. `registerAllTools(server, db)` stable across Tasks 32, 33.

**Type consistency post-refactor:**
- Drizzle tables live in `@agent-brain/shared/src/db/schema.ts` (Task 3).
- Zod entity schemas via `createSelectSchema(projects)` etc. in Task 4 — camelCase throughout.
- MCP handlers (Tasks 19–31) import tables from `@agent-brain/shared/db/schema` and use them as Drizzle query builders (`db.insert(notes).values({ projectId, createdAt, ... })`). No hand-written snake_case → camelCase conversion anywhere. Gotcha 3 becomes structurally impossible.
- `drizzle-zod` is a real runtime dependency of `@agent-brain/shared` (used by `createSelectSchema` in Task 4) — not dead weight.

---

## Execution handoff

**Plan complete and saved to `docs/superpowers/plans/2026-04-08-agent-brain-mcp-backend.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration.

**2. Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints.

**Which approach?**

**Plan B (UI) is deferred** — it will be written as a separate plan (`2026-04-<TBD>-agent-brain-ui.md`) after Plan A is merged and smoke-tested end-to-end via Claude Code.
